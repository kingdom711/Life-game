# 아이템 시스템 2.0 - 스탯 및 검교정(강화) 로직

> **작성일**: 2024-12-23  
> **버전**: 2.0.0  
> **상태**: ✅ 구현 완료

---

## 📋 개요

### 개발 목표 및 게이미피케이션 전략

**핵심 목적**: 단순 치장용(Cosmetic) 아이템을 **기능성 장비(Functional Gear)**로 전환하여, 사용자의 장기적인 성장 욕구 자극 및 포인트(P) 소비처(Sink) 확보.

### 메타 게임 요소 (Identity-Based Progression)

| 요소 | 설명 |
|------|------|
| **검교정(Calibration)** | RPG의 '강화' 시스템을 산업 안전 용어인 '검교정'으로 재해석. 포인트를 소모하여 장비의 성능(보상 효율)을 최적화함. |
| **안전 스탯(Safety Stats)** | 실제 안전 점검 의무는 건드리지 않되, 보상 획득량(Point/XP)과 편의성(Streak Protection)에 관여하는 스탯 부여. |
| **세트 효과(Set Bonus)** | 동일 세트 아이템을 2/4/7개 장착 시 추가 보너스 스탯 및 시각 효과 적용. |

---

## 📊 데이터 모델 및 스키마

### A. Item Schema (Base Definition)

`src/data/itemsData.js`

```javascript
{
  id: "item_helmet_001",
  name: "Standard Safety Helmet",
  type: "HELMET",
  rarity: "RARE", // COMMON, RARE, EPIC, LEGENDARY
  
  // [New] 기본 스탯 정의 (Base Stats)
  baseStats: {
    pointBoost: 5,      // 포인트 획득량 +5%
    xpAccelerator: 0,   // 경험치 획득량 +0%
    streakSaver: 0      // 스트릭 보호 확률 0%
  },
  
  // [New] 검교정(강화) 설정
  calibrationConfig: {
    maxLevel: 10,           // 최대 강화 수치
    costPerLevel: 500,      // 강화 1회당 소모 포인트
    successRateBase: 0.9,   // 초기 성공 확률 (90%)
    statIncrement: 1.5      // 레벨당 스탯 증가폭 (%)
  },
  
  // [New] 세트 효과 ID
  setId: "set_neon_guardian",
  
  imageUrl: "/assets/items/helmet_rare.png"
}
```

### B. UserInventory Schema (Instance Data)

`src/utils/storage.js` - `userInventoryInstances`

사용자가 소유한 '개별 아이템'의 상태를 추적합니다.

```javascript
{
  instanceId: "uuid_v4_generated",
  userId: "user_001",
  itemId: "item_helmet_001",  // Base Item 참조
  
  // [New] 현재 성장 상태
  currentCalibrationLevel: 3,  // 현재 +3강
  
  // [New] 최종 계산된 활성 스탯 (Base + Calibration Bonus)
  activeStats: {
    pointBoost: 9.5,     // 5 + (3 * 1.5)
    xpAccelerator: 0,
    streakSaver: 0
  },
  
  isEquipped: true,
  acquiredAt: "2024-12-24T10:00:00Z"
}
```

### C. Set Schema

`src/data/setsData.js`

```javascript
{
  id: "set_neon_guardian",
  name: "네온 수호자 세트",
  bonuses: [
    {
      pieces: 2,
      stats: { pointBoost: 5, xpAccelerator: 0, streakSaver: 0 },
      description: "2세트 효과: 포인트 획득량 +5%"
    },
    {
      pieces: 4,
      stats: { pointBoost: 10, xpAccelerator: 5, streakSaver: 0 },
      description: "4세트 효과: 포인트 획득량 +10%, 경험치 획득량 +5%"
    },
    {
      pieces: 7,
      stats: { pointBoost: 15, xpAccelerator: 10, streakSaver: 5 },
      description: "7세트 효과: 포인트 획득량 +15%, 경험치 획득량 +10%, 스트릭 보호 확률 +5%"
    }
  ],
  visualAura: "HOLOGRAM_SHIELD_V2"
}
```

---

## 🔧 핵심 서비스 로직

### A. 검교정 서비스 (calibrationService.js)

```javascript
import { getItemById } from '../data/itemsData';
import { userInventoryInstances, points, userProfile } from './storage';

export const calibrateItem = (instanceId) => {
  const userId = userProfile.getName();
  const itemInstance = userInventoryInstances.getItemByInstanceId(userId, instanceId);
  const baseItem = getItemById(itemInstance.itemId);
  
  const { maxLevel, costPerLevel, successRateBase, statIncrement } = baseItem.calibrationConfig;
  
  // 최대 레벨 체크
  if (itemInstance.currentCalibrationLevel >= maxLevel) {
    return { success: false, message: "최대 레벨 도달" };
  }
  
  // 포인트 체크 및 소모
  if (!points.canAfford(costPerLevel)) {
    return { success: false, message: "포인트 부족" };
  }
  points.subtract(costPerLevel);
  
  // 성공 확률 계산 (레벨당 -5%)
  const currentLevel = itemInstance.currentCalibrationLevel;
  const successRate = successRateBase - (currentLevel * 0.05);
  const isSuccess = Math.random() < successRate;
  
  if (isSuccess) {
    const newLevel = currentLevel + 1;
    const newActiveStats = {
      pointBoost: baseItem.baseStats.pointBoost + (newLevel * statIncrement),
      xpAccelerator: baseItem.baseStats.xpAccelerator + (newLevel * statIncrement),
      streakSaver: baseItem.baseStats.streakSaver + (newLevel * statIncrement)
    };
    
    userInventoryInstances.updateItem(userId, instanceId, {
      currentCalibrationLevel: newLevel,
      activeStats: newActiveStats
    });
    
    return { status: "success", isSuccess: true, newLevel, effect: "Visual_Success_Lime_Green" };
  }
  
  return { status: "success", isSuccess: false, effect: "Visual_Failure_Red" };
};
```

### B. 스탯 계산 로직 (pointsCalculator.js)

```javascript
// 전체 활성 스탯 계산 (장비 + 세트 효과)
export const getTotalActiveStats = () => {
  const userId = userProfile.getName();
  const equippedInstances = userInventoryInstances.getEquippedItemsByUserId(userId);
  
  let totalPointBoost = 0;
  let totalXpAccelerator = 0;
  let totalStreakSaver = 0;
  const equippedSetItems = {}; // { setId: count }
  
  // 1. 개별 아이템 스탯 합산
  equippedInstances.forEach(instance => {
    const activeStats = calculateItemActiveStats(instance);
    totalPointBoost += activeStats.pointBoost;
    totalXpAccelerator += activeStats.xpAccelerator;
    totalStreakSaver += activeStats.streakSaver;
    
    const baseItem = getItemById(instance.itemId);
    if (baseItem?.setId) {
      equippedSetItems[baseItem.setId] = (equippedSetItems[baseItem.setId] || 0) + 1;
    }
  });
  
  // 2. 세트 효과 추가
  const activeSetBonuses = [];
  let visualAura = null;
  
  Object.entries(equippedSetItems).forEach(([setId, count]) => {
    const set = getSetById(setId);
    if (set) {
      set.bonuses.forEach(bonus => {
        if (count >= bonus.pieces) {
          totalPointBoost += bonus.stats.pointBoost;
          totalXpAccelerator += bonus.stats.xpAccelerator;
          totalStreakSaver += bonus.stats.streakSaver;
          activeSetBonuses.push(set.name);
          if (set.visualAura) visualAura = set.visualAura;
        }
      });
    }
  });
  
  return { totalPointBoost, totalXpAccelerator, totalStreakSaver, activeSetBonuses, visualAura };
};
```

---

## 🌐 API 명세서 (RESTful Specification)

### A. 장비 검교정 (강화) 요청

**Endpoint**: `POST /api/v1/inventory/calibrate`

**Description**: 포인트를 소모하여 아이템의 레벨을 올리고 스탯을 재계산합니다.

**Request Body**:
```json
{
  "userId": "user_123",
  "instanceId": "uuid_of_user_item"
}
```

**Business Logic (Backend)**:
1. User의 보유 포인트 잔액 확인 (부족 시 402 Payment Required)
2. `calibrationConfig`의 성공 확률(RNG) 계산
3. 성공 시: `currentCalibrationLevel` +1, `activeStats` 재계산, 포인트 차감
4. 실패 시: 포인트만 차감

**Response (Success)**:
```json
{
  "status": "success",
  "newLevel": 4,
  "pointsConsumed": 500,
  "isSuccess": true,
  "effect": "Visual_Success_Lime_Green"
}
```

### B. 전체 스탯 조회 (HUD Display용)

**Endpoint**: `GET /api/v1/user/{userId}/stats/active`

**Description**: 현재 장착된 모든 장비의 스탯 합계를 반환합니다 (세트 효과 포함).

**Response**:
```json
{
  "totalPointBoost": 15.5,
  "totalXpAccelerator": 5.0,
  "totalStreakSaver": 2.0,
  "activeSetBonuses": ["set_neon_guardian"],
  "visualAura": "HOLOGRAM_SHIELD_V2"
}
```

---

## 🎨 UI/UX 디자인

### 컨셉: "High-Tech Lab"

깨끗한 화이트 톤 실험실에서 정밀 장비를 튜닝하는 느낌.

### 검교정 화면 (CalibrationModal)

| 요소 | 디자인 |
|------|--------|
| **배경** | Pure White (#FFFFFF) + Subtle Grid Pattern (#F0F0F0) |
| **아이템 표시** | 공중에 부유(Floating)하며 천천히 회전 |
| **수치 표시** | 현재 스탯(Black Text) → 예상 스탯(Cyan Neon Text + Blink Animation) |
| **강화 버튼** | "CALIBRATE [500 P]" - 호버 시 Magenta Neon 라인 |
| **성공 연출** | Bright Lime (#32CD32) 파동 + "CALIBRATION COMPLETE" 홀로그램 |

### 색상 팔레트

| 용도 | 색상 코드 |
|------|-----------|
| Neon Cyan | `#00FFFF` |
| Neon Magenta | `#FF00FF` |
| Bright Lime (성공) | `#32CD32` |
| Danger Red (실패) | `#FF4444` |
| Pure White | `#FFFFFF` |
| Grid Pattern | `#F0F0F0` |

---

## 📁 파일 구조

```
src/
├── data/
│   ├── itemsData.js       # 아이템 정의 + baseStats, calibrationConfig
│   └── setsData.js        # 세트 효과 정의 (NEW)
├── utils/
│   ├── storage.js         # userInventoryInstances 추가
│   ├── calibrationService.js  # 검교정 로직 (NEW)
│   ├── pointsCalculator.js    # getTotalActiveStats() 추가
│   ├── inventoryManager.js    # 인스턴스 기반으로 리팩토링
│   └── dataMigration.js       # 데이터 마이그레이션 (NEW)
├── components/
│   ├── CalibrationModal.jsx   # 검교정 UI (NEW)
│   └── StatsHUD.jsx           # 활성 스탯 표시 (NEW)
├── pages/
│   └── Inventory.jsx          # 검교정 버튼 통합
└── styles/
    └── features/item-systems.css  # Neon Lab 스타일 추가 (2026-02-13 분리)
```

---

## 🔄 데이터 마이그레이션

### 기존 데이터 → 신규 스키마 변환

`dataMigration.js`가 앱 초기화 시 자동 실행:

1. 기존 `inventory` 배열의 아이템 → `userInventoryInstances`로 변환
2. 기존 `equippedItems` 객체 → 인스턴스에 `isEquipped: true` 설정
3. `enhancementLevel` → `currentCalibrationLevel`로 매핑
4. 기본 스탯 계산하여 `activeStats` 초기화
5. 마이그레이션 완료 후 기존 데이터 정리

---

## ✅ 구현 완료 항목

| 영역 | 파일 | 상태 |
|------|------|------|
| 데이터 스키마 | `itemsData.js` | ✅ 완료 |
| 세트 효과 | `setsData.js` | ✅ 완료 |
| 스토리지 | `storage.js` | ✅ 완료 |
| 검교정 서비스 | `calibrationService.js` | ✅ 완료 |
| 스탯 계산 | `pointsCalculator.js` | ✅ 완료 |
| API 레이어 | `inventoryApi.js` | ✅ 완료 |
| 검교정 모달 | `CalibrationModal.jsx` | ✅ 완료 |
| 스탯 HUD | `StatsHUD.jsx` | ✅ 완료 |
| 인벤토리 페이지 | `Inventory.jsx` | ✅ 완료 |
| 아바타 효과 | `AvatarWindow.jsx` | ✅ 완료 |
| CSS 스타일 | `src/styles/features/item-systems.css` | ✅ 완료 |
| 데이터 마이그레이션 | `dataMigration.js` | ✅ 완료 |
| 인벤토리 매니저 | `inventoryManager.js` | ✅ 완료 |

---

## 🔮 향후 확장 가능성

### 1. 내구도 시스템 (미구현)
- `currentDurability`: 사용에 따른 내구도 감소
- 수리 기능 추가

### 2. 강화 보호권
- 실패 시 레벨 하락 방지 아이템

### 3. 축복/저주 시스템
- 특수 효과 부여

### 4. 거래소
- 유저 간 아이템 거래

---

**마지막 업데이트**: 2024-12-23  
**작성자**: 개발팀  
**버전**: 2.0.0

